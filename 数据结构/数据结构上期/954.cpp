#include <bits/stdc++.h>
using namespace std;
int n;
char ch;
int main() {
    cin >> n;
    while (n--) {
        cin >> ch;
        cout << ch << ' ';
    }
    cin >> n;
    while (n--) {
        cin >> ch;
        cout << ch << ' ';
    }
    return 0;
}